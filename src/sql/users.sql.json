{
	"TABLE_NAME": "users",
	"CREATE_TABLE": "CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT, email TEXT, password TEXT, roleId INTEGER, createdAt DATETIME, updatedAt DATETIME)",
	"SELECT_USER_BY_EMAIL": "SELECT * FROM users WHERE email = $email",
	"SELECT_USER_BY_ID": "SELECT * FROM users WHERE id = $id",
	"INSERT_USER": "INSERT INTO users (email, password, roleId, createdAt, updatedAt) VALUES ($email, $password, $roleId, $createdAt, $updatedAt)"
}
