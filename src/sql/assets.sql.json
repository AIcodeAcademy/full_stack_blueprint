{
	"TABLE": "assets",
	"CREATE_TABLE": "CREATE TABLE IF NOT EXISTS assets (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, category_id INTEGER NOT NULL, name TEXT NOT NULL, value DECIMAL(15,2) NOT NULL, quantity DECIMAL(15,2) NOT NULL, acquisition_date DATE NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(category_id) REFERENCES categories(id), FOREIGN KEY(user_id) REFERENCES users(id))",
	"SELECT_ALL": "SELECT * FROM assets",
	"SELECT_BY_ID": "SELECT * FROM assets WHERE id = $id",
	"SELECT_BY_FIELD": "SELECT * FROM assets WHERE $field = $value",
	"SELECT_BY_QUERY": "SELECT * FROM assets WHERE $query",
	"SELECT_BY_USER_ID": "SELECT * FROM assets WHERE user_id = $user_id",
	"INSERT": "INSERT INTO assets (user_id, category_id, name, value, quantity, acquisition_date, created_at, updated_at) VALUES ($user_id, $category_id, $name, $value, $quantity, $acquisition_date, $created_at, $updated_at)",
	"UPDATE": "UPDATE assets SET category_id = $category_id, name = $name, value = $value, quantity = $quantity, acquisition_date = $acquisition_date, updated_at = $updated_at WHERE id = $id AND user_id = $user_id",
	"DELETE": "DELETE FROM assets WHERE id = $id AND user_id = $user_id",
	"SEED": []
}
